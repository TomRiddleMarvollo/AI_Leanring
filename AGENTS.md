# AGENTS.md

> Hướng dẫn cho **mọi AI coding agent** làm việc trong repo này (Codex, Cursor,
> và các agent đọc `AGENTS.md`). File này **được commit công khai** — khác với
> harness riêng `CLAUDE.md` (không commit).
> _Instructions for any AI coding agent working in this repo. Committed & shared._

## Đọc trước khi sinh code — Read first
1. [CODEMAP.md](CODEMAP.md) — bản đồ repo: "muốn sửa X thì vào file Y".
2. [CONVENTIONS.md](CONVENTIONS.md) — quy ước code (nguồn chuẩn).
3. [docs/GLOSSARY.md](docs/GLOSSARY.md) — thuật ngữ Việt/Anh, để đặt tên nhất quán.
4. [docs/STRUCTURE.md](docs/STRUCTURE.md) — cấu trúc thư mục & luồng dữ liệu.

## Nguyên tắc bắt buộc — Core rules
- **Code phải để người bảo trì được, không cần AI agent.** Ưu tiên code tường minh,
  "nhàm chán"; không over-engineer (xem "Boring code" trong CONVENTIONS.md).
- **Theo đúng quy ước** trong CONVENTIONS.md (naming, kích thước file/hàm, phụ thuộc một chiều).
- **i18n:** không hardcode chuỗi hiển thị; mỗi key có cả `vi` và `en`.
- **Responsive (web):** ứng dụng web mặc định **responsive** — tự thích ứng mobile/tablet/desktop, không bể layout, không tràn ngang. Mobile-first, flex/grid + đơn vị tương đối, có viewport meta. Xem CONVENTIONS.md §10.
- **Icons:** mặc định dùng **Tabler Icons** (MIT) — cần icon thì lấy từ Tabler; không trộn bộ khác/proprietary. Xem CONVENTIONS.md §11.
- **Dependency:** ưu tiên mã nguồn mở, **chỉ MIT hoặc Apache 2.0**. Kiểm tra license
  trước khi thêm; không phải MIT/Apache-2.0 → dừng lại và hỏi.
- **An toàn & bảo mật:** không hardcode secret; validate input ở biên; hash mật khẩu;
  kiểm quyền ở backend; chống injection. **Có login → BẮT BUỘC 2FA: mật khẩu + OTP qua email**
  (OTP hết hạn ngắn, một lần, rate-limit). Xem [SECURITY.md](SECURITY.md). Không commit/push trừ khi được yêu cầu; không chạy
  lệnh phá hủy khi chưa xác nhận. Chạy `check-secrets.sh` / `check-deps-security.sh` /
  `check-sast.sh`. Chi tiết: [SECURITY.md](SECURITY.md).
- **Diff nhỏ, có test** cho logic quan trọng.
- **Header hàm chuẩn:** mọi hàm public có docstring/JSDoc gồm tóm tắt + `@trace <ID>` +
  `@version <x.y.z>` (+ `@param/@returns/@throws`). **Code tới đâu cập nhật header tới đó**,
  sửa hàm thì bump `@version`. Ép bởi `check-fn-doc.sh`. Xem CONVENTIONS.md §4.
- **Viết code dễ trích xuất thành thư viện:** logic tái dùng đặt trong `*/lib/<pkg>/` (hoặc
  `packages/<pkg>/`), **tự đủ** (types/tests/README riêng, một cửa `index`), **không import ngược
  vào app** (`routes`/`services`/`store`/`config`...), phụ thuộc *truyền vào* (DI). Chi tiết:
  CONVENTIONS.md §9. Kiểm tra: `bash scripts/check-lib-boundaries.sh`. Trích xuất: [docs/EXTRACTION.md](docs/EXTRACTION.md).

## Lệnh dự án — Commands
> Phần giữa hai mốc AUTO được **sinh tự động** từ `package.json`/`requirements.txt`
> bằng `bash scripts/gen-commands.sh`. **Đừng sửa tay** phần đó.
> _Auto-generated by scripts/gen-commands.sh — don't edit between the AUTO markers._
<!-- AUTO-COMMANDS:START -->
- Install: `cd src/frontend && npm install` · `cd src/backend && pip install -r requirements.txt`
- Run: `make dev` (Docker, khuyến nghị) hoặc `make dev-local` (chạy trực tiếp, không Docker)
- Test: `make test` (Docker) hoặc `make test-local`
<!-- AUTO-COMMANDS:END -->
- Kiểm tra license / License check: `bash scripts/check-licenses-node.sh` · `... -python.sh`

## Tài liệu SDLC (quy trình chặt) — SDLC docs (strict process)
> Dự án này yêu cầu bộ tài liệu vòng đời **luôn up-to-date và nhất quán**, liên kết
> bằng ID truy vết. Xem [v-model/README.md](v-model/README.md).

- **Nhanh:** `bash scripts/new-feature.sh "Tên tính năng" [--nfr] [--screen]` sinh stub liên kết đủ chuỗi (ID khớp sẵn) → chỉ việc điền `_(TODO)_`.
- **BẮT BUỘC — Khi thêm/đổi một tính năng, ĐI ĐỦ CHUỖI** (đừng bỏ mức nào):
  `REQ` (HLR) → `SRS` (đặc tả chi tiết) + `NFR` (phi chức năng có số đo) → `ARC` (SAD, + Threat model) → `DD` (SDD) → code (`@trace DD-###`).
  **Kiểm chứng V-model (4 phase test, mỗi phase đủ strategy/plan/spec/cases/report):**
  Unit `UT`↔`DD` · Integration `IT`↔`ARC` · SW-Qualification `QT`↔`SRS` · **Acceptance `AT`↔`REQ`**. Mỗi `NFR` **trích REQ gốc** (cột Traces ↑) và có ≥1 test. Giao nộp: User Manual + Operations.
- **Giữ truy vết liền mạch:** mỗi `REQ` có `SRS` **và** `AT`; mỗi `SRS` có `ARC` **và** `QT`; mỗi `ARC` có `DD` **và** `IT`; mỗi `DD` được code tham chiếu **và** có `UT`; mỗi `NFR` trích `REQ`, **được SAD giải quyết**, **và** có ≥1 test (ASPICE 4.0: NFR trace đủ cả REQ lẫn ARC).
- **Grounding (chống bịa):** mỗi item SRS/ARC/DD/test PHẢI trích ID upstream (SRS→REQ, ARC→SRS, DD→ARC, TC→cấp thiết kế). Không có gốc → gắn `inferred` + người duyệt. Ép: `check-grounding.sh`.
- **Test docs ↔ test code:** mỗi `UT-###` đã claim trong test-cases.md phải có **test code thật** mang ID đó (`check-test-trace.sh` chặn); IT/QT khuyến khích (warn); test tay có chủ đích → ghi `manual` + người duyệt. Chạy test/coverage: `check-test-coverage.sh`.
- **Code ↔ static analysis:** SAST (bandit/eslint-security/ruff — MIT/Apache, KHÔNG SonarQube/semgrep vì copyleft) phải chạy; **cấm suppress không lý do** (`# nosec`/`eslint-disable`... cần lý do rõ + người duyệt). Ép: `check-suppressions.sh`.
- **Tầng 0 (edit-time):** mỗi lần bạn sửa file, hook `.claude/settings.json` tự chạy check nhanh liên quan (i18n/grounding/traceability/suppressions/size). Nhận feedback ⛔ → **sửa ngay** rồi mới làm tiếp, đừng để dồn tới pre-commit.
- **KHÔNG dùng `git commit --no-verify` / `-n`** để bỏ qua hook. Hook chặn là vì trace/tài liệu chưa đủ — hãy sửa cho đủ rồi commit, không bypass.
- **Sửa ở đâu, đồng bộ ở đó:** đổi một mục → rà tất cả tài liệu liên quan ngược & xuôi.
- **Cấu trúc V-model:** tài liệu nằm trong `v-model/<phase>/` (mỗi phase 1 folder + `review-checklist.md` + `review-report.md`). Checklist tick HẾT mới ĐẠT chất lượng.
- **KHÔNG override tài liệu phase** — chỉ chỉnh tại chỗ cho đúng-đủ-không thừa-không thiếu.
- **Mọi thay đổi FE/BE phải phản ánh ĐỦ vào REQ/SRS** (chi tiết tăng dần): REQ=tính năng; SRS=use case cụ thể **+ đặc tả trường/nút/giao diện/từng trạng thái**. Xem [v-model/README.md](v-model/README.md).
- **Definition of Done (ĐỦ mới tính xong) — chống làm nông:**
  - Thiết kế (`DD-###`): mỗi mục phải có **đủ 7 tiểu mục** — Functions/APIs (liệt kê ĐỦ),
    Happy path, **Error & alternate flows** (từng ca lỗi), **Sequence diagram**, Validation,
    **Security**, Data model. **Hợp đồng mỗi hàm/endpoint**: đủ ĐẦU VÀO · ĐẦU RA · **CA LỖI** (raise/throw/4xx/5xx).
  - Yêu cầu (`SRS-###`): viết theo **EARS** — câu *hệ thống **phải/shall** \<đáp ứng\>*, có mệnh đề kích hoạt (Khi/Nếu/Trong khi/Ở nơi) khi phù hợp; tránh danh từ trơ không test được.
  - User Manual (`SCR-###`): **một mục cho MỌI màn hình**, đủ Purpose / Steps / **Transitions
    (mọi lối vào-ra)** / Error & empty states.
  - Không viết "vài biểu hiện chung chung" — phải đầy đủ function/API, mọi luồng lỗi, mọi transition.
  - **UM phủ đủ 3 trục — KHÔNG sót:** mọi **màn hình** (route), mọi **button/action** (nhãn i18n), mọi **use-case** (SRS). `gen-ground-truth.sh` liệt kê tập đầy đủ; `check-completeness.sh` cảnh báo nút chưa được hướng dẫn; `review-docs-llm.sh` (LLM) soi màn hình/button/use-case còn thiếu.
- **Biểu đồ (BẮT BUỘC) — Diagrams:**
  - Vẽ bằng **Mermaid**; **mỗi sơ đồ bắt đầu bằng YAML frontmatter** (`---` title/config).
  - **Kiến trúc tổng quát:** ưu tiên **C4 model** (`C4Context`/`C4Container`/`C4Component`).
  - **Kế đó:** **Biểu đồ khối** (`block-beta`).
  - **Hoạt động một tính năng:** **Sequence diagram** (`sequenceDiagram`) trong từng `DD-###`.
- **Bắt buộc tự phản biện (self-critique) trước khi báo xong:** tự hỏi "còn thiếu màn hình /
  endpoint / luồng lỗi / ca biên nào không?" và bổ sung, rồi chạy:
  ```
  bash scripts/verify-all.sh          # MỘT lệnh: gen + MỌI check, in scorecard ✓/✗/⚠
  bash scripts/verify-all.sh --full   # khi giao nộp: thêm lint/SAST/deps/coverage + LLM delta
  ```
  Phải **0 lỗi** (và xử lý hết "gaps" do LLM nêu) mới coi là xong. Đối chiếu
  [v-model/_traceability/GROUND-TRUTH.md](v-model/_traceability/GROUND-TRUTH.md): mọi error code / endpoint / màn hình phải được phủ.

## Khi thực hiện BẤT KỲ thay đổi nào — Checklist bắt buộc
> Áp dụng cho mọi thay đổi code, không chỉ khi "tạo tài liệu".

1. **i18n khi chạm giao diện (vi/en):** Sửa gì liên quan UI → kiểm tra **mọi chuỗi hiển thị
   có đủ cả `vi` và `en`** trong `locales/`; **không hardcode** chuỗi; đối chiếu key `vi` ↔ `en`
   khớp nhau. Chạy `bash scripts/check-i18n.sh` (parity được CI **chặn** nếu lệch key).
2. **Impact analysis + đồng bộ Frontend/Backend:** TRƯỚC khi sửa, xác định thay đổi lan tới đâu
   (frontend, backend, `shared/types`, DB, hợp đồng API, i18n, tài liệu). Nếu đổi API/contract/kiểu
   dữ liệu → cập nhật **đồng bộ cả hai phía** trong cùng thay đổi (BE: routes/schemas/services;
   FE: services/types/UI) — **không để lệch** một bên.
3. **Cập nhật tài liệu & ghi chú liên quan để lần sau sinh tài liệu ĐÚNG & ĐỦ:** sau thay đổi,
   cập nhật mắt xích SDLC bị ảnh hưởng để truy vết không đứt và tài liệu không cũ —
   `REQ → SRS(+NFR) → SAD(ARC) → SDD(DD) → code trace (`@trace DD-###`)` → 4 phase test (Unit `UT`↔DD ·
   Integration `IT`↔ARC · SW-Qual `QT`↔SRS · Acceptance `AT`↔REQ) → User Manual + Operations. Cập nhật thêm `CODEMAP.md`, `docs/GLOSSARY.md`, ghi chú/“memory” dự án nếu
   cấu trúc hoặc thuật ngữ thay đổi. Rồi chạy:
   ```
   bash scripts/verify-all.sh   # MỘT lệnh: gen + mọi check — scorecard 0 ✗ mới tính xong
   ```

4. **Ghi Agent Journal (nếu có `agent-journal/`):** tại các checkpoint (ra quyết định, gặp lỗi,
   khắc phục, hiểu ra cách làm đúng, tạo tool, cuối phiên) → **append entry** vào
   `agent-journal/journal/JOURNAL.md` đúng định dạng (`[decision|problem|fix|howto|tool]`). Tool mới
   → thêm `agent-journal/tools/<tên>/README.md`. Quyết định/pitfall/cách-làm-đúng đáng nhớ → chưng
   cất sang `agent-journal/knowledge/*.md` (supersede-có-vết). Convention: `agent-journal/README.md`.

## Sau khi sửa — After changes
- Chạy lint/test/build nếu có; báo cáo kết quả thật.
- Cập nhật [CODEMAP.md](CODEMAP.md) nếu thêm/di chuyển thành phần lớn.
- Quyết định kiến trúc lớn → ghi một ADR trong `docs/adr/`.
- Ghi lại checkpoint đáng nhớ vào `agent-journal/` (xem checklist mục 4) — để phiên sau không mất bối cảnh.
